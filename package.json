{
  "name": "truncate-text-dom",
  "version": "0.1.4",
  "description": "Function for finely-tuned text truncation in the DOM.",
  "main": "index.js",
  "license": "MIT",
  "repository": "letam/truncate-text-dom",
  "scripts": {
    "dev": "vite",
    "lint": "eslint ./src",
    "storybook": "ladle serve",
    "build-storybook": "ladle build",
    "preview-storybook": "ladle preview"
  },
  "devDependencies": {
    "@ladle/react": "^2.2.2",
    "eslint": "^8.8.0",
    "eslint-config-react-app": "^7.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "overrides": [
      {
        "files": [
          "**/*.stories.*"
        ],
        "rules": {
          "import/no-anonymous-default-export": "off"
        }
      }
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
